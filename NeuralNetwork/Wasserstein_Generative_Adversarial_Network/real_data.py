import numpy as np 
import matplotlib.pyplot as plt 
import pickle 




"""
def polar_coordinates(r, theta):
    r1 = r + np.random.normal(0, 1) * 7
    r2 = r + np.random.normal(0, 1) * 7
    x = r1 * np.cos(theta)
    y = r2 * np.sin(theta)

    return [x, y]

r = 50 
data = []
for theta in np.arange(0, 2*np.pi, 0.01):
    a = polar_coordinates(r, theta)
    data.append(a)
    
    


with open("circle_scatter.pickle", "wb") as f:
    pickle.dump(data, f)"""